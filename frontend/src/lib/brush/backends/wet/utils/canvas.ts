// Thin re-exports so wet backend stays in sync with your shared canvas helpers.
export type { Ctx2D, CanvasLike } from "@/lib/canvas/context";
export { createLayer, get2DContext } from "@/lib/canvas/context";
